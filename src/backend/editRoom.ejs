<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Room - Online Book Rental</title>
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
            padding-top: 70px;
        }

        header {
            background-color: #333;
            color: #fff;
            padding: 1rem;
            position: fixed;
            top: 0;
            width: 100%;
            height: 10%;
            z-index: 1000;
        }

        .header-title {
            margin: 0;
        }

        .navbar-nav .nav-link {
            color: #fff !important;
        }

        .product-item {
            background-color: #fff;
            border: 1px solid #ccc;
            padding: 1rem;
            border-radius: 5px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            text-align: center;
            margin-bottom: 1rem;
            display: flex;
            flex-direction: column;
            justify-content: space-between;
            height: 100%;
        }

        .product-item img {
            max-width: 100%;
            height: auto;
            border-radius: 5px;
            margin-bottom: 1rem;
        }

        .product-item h2 {
            font-size: 1.2rem;
            margin-bottom: 0.5rem;
        }

        .product-item .btn {
            padding: 0.5rem 1rem;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            text-decoration: none;
            font-size: 0.9rem;
            margin: 0.5rem 0;
        }

        .product-item .edit-btn {
            background-color: #4CAF50;
            color: #fff;
        }

        .product-item .delete-btn {
            background-color: #f44336;
            color: #fff;
        }

        footer {
            background-color: #333;
            color: #fff;
            padding: 1rem;
            text-align: center;
            margin-top: 2rem;
        }

        .btn-back {
            margin-bottom: 20px;
        }
    </style>
</head>

<body>
    <header>
        <div class="container">
            <h1 class="header-title">Edit Room</h1>
            <nav class="navbar navbar-expand-lg navbar-dark">
            </nav>
        </div>
    </header>

    <div class="container my-4">
        <a href="../backend" class="btn btn-secondary btn-back">Back</a>
    </div>

    <main class="container mt-5">
        <div class="row">
            <% product.forEach(products => { %>
            <div class="col-sm-6 col-md-4 col-lg-3">
                <div class="product-item">
                    <img src="<%= products.image %>" alt="<%= products.productTitle %>">
                    <h2><a href="/backend/editBook/<%= products.productTitle %>"><%= products.productTitle %></a></h2>
                    <div class="d-flex justify-content-between">
                        <form action="/deleteBook/<%= products.id %>" method="POST" class="flex-fill mr-1">
                            <button type="submit" class="btn delete-btn btn-block">DELETE</button>
                        </form>
                        <a href="/backend/editBook/<%= products.productTitle %>" class="btn edit-btn flex-fill ml-1 btn-block">Edit</a>
                    </div>
                </div>
            </div>
            <% }); %>
        </div>
    </main>

    <footer>
        <p>&copy; 2024 Online Book Rental</p>
    </footer>

    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.5.2/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>

</html>
